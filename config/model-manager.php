<?php
declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | Model guards
    |--------------------------------------------------------------------------
    |
    | Проверки, через которые проходит класс модели при взятии её под управление
    | ModelManager. Нужны пакетам, которые пишут в базу в обход менеджера и
    | потому требуют на модели особой обвязки: модель, забывшая её подключить,
    | падает сразу, а не теряет данные при откате транзакции.
    |
    | Каждый класс реализует App\Core\ModelManager\Guards\ManagedModelGuard.
    | Проверки выполняются один раз на класс модели.
    |
    */

    'guards' => [
        App\Core\ModelManager\Guards\MediaLibraryIntegrationGuard::class,
    ],

];
